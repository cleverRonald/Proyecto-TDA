<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.dlanor.proyectotda">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Aplicacion">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".InicioSesion" />
        <activity android:name=".Registro" />
        <activity android:name=".Agenda" />
        <activity android:name=".Edit_agenda" />
        <activity android:name=".Horario" />
        <activity android:name=".Edit_horario" />
        <activity android:name=".Recordatorio" />
        <activity android:name=".Edit_recordatorio"></activity>
    </application>

</manifest>